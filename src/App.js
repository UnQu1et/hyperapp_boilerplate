import { h } from 'hyperapp';
import './App.css';

const App = () => (
  <div className="App">
    <header className="App-header">
      <span className="App-logo">Ты пидор!</span>
      <h1 className="App-title">Это hyperapp, сучка!</h1>
    </header>
    <p className="App-intro">
      To get started, edit <code>src/App.js</code> and save to reload.
    </p>
  </div>
);

export default App;
